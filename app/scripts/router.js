Highwire.Router.map(function () {
  // Add your routes here
  this.route("build", {
    path:"/build"
  });
  this.route("queue", {
    path:"/queue"
  });
  this.route("logs", {
    path:"/logs"
  });
});
